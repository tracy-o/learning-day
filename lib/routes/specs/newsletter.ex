defmodule Routes.Specs.Newsletter do
  def specification do
    %{
      specs: %{
        slack_channel: "#help-topics",
        runbook: "TBD",
        platform: "Webcore",
        personalisation: "on"
      }
    }
  end
end
